/**
 * New Booking Tab Placeholder
 * Empty component used as placeholder for NewBookingTab
 * Actual navigation is handled via listeners in BottomTabNavigator
 */

import React from 'react';
import { View } from 'react-native';

const NewBookingTabPlaceholder = () => {
  return <View style={{ flex: 1 }} />;
};

export default NewBookingTabPlaceholder;

